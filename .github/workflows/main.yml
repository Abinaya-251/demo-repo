on: push
jobs:
  first-jobb:
    runs-on: ubuntu-latest 
    steps:
      - run: npm install -g npm@latest
      - run: node --version
      - run: npm --version
      - run: npm install
      - uses: actions/checkout@v2
  
